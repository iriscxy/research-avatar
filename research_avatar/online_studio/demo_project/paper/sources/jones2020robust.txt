    Robust Encodings: A Framework for Combating Adversarial Typos


                 Erik Jones Robin Jia∗ Aditi Raghunathan∗ Percy Liang
                       Computer Science Department, Stanford University
                 {erjones,robinjia,aditir,pliang}@stanford.edu




                                                                    Sentences             Encodings          Predictions
                      Abstract
                                                                    Irnspired atcing
    Despite excellent performance on many tasks,                                           Inspired acting   Positive

    NLP systems are easily fooled by small adver-               Inspired acting
    sarial perturbations of inputs. Existing pro-                   Inspikred ating
    cedures to defend against such perturbations                                           Inspikred ating   Positive

    are either (i) heuristic in nature and suscep-
                                                                   Copmlete fljop
    tible to stronger attacks or (ii) provide guar-
    anteed robustness to worst-case attacks, but                                           Complete flop     Negative
                                                                  Complete flop
    are incompatible with state-of-the-art mod-
    els like BERT. In this work, we introduce ro-
    bust encodings (RobEn): a simple framework                Figure 1: Example of a defense using RobEn. An
    that confers guaranteed robustness, without               adversary can perturb sentences (blue, underlined)
    making compromises on model architecture.                 to many different perturbations (red, not-underlined)
    The core component of RobEn is an encoding                within the attack surface (red, ovals). We define an en-
    function, which maps sentences to a smaller,              coding function α such that each perturbation of the
    discrete space of encodings. Systems using                input sentences maps to one of a few encodings (grey,
    these encodings as a bottleneck confer guaran-            rounded rectangles). We can then use any model g to
    teed robustness with standard training, and the           make predictions given the encodings.
    same encodings can be used across multiple
    tasks. We identify two desiderata to construct
    robust encoding functions: perturbations of a             (Ebrahimi et al., 2018a), and spam filtering (Lee
    sentence should map to a small set of encod-              and Ng, 2005), among others.
    ings (stability), and models using encodings                  We aim to build systems that achieve high ro-
    should still perform well (fidelity). We instan-          bust accuracy: accuracy against worst-case attacks.
    tiate RobEn to defend against a large family
                                                              Broadly, existing methods to build robust mod-
    of adversarial typos. Across six tasks from
    GLUE, our instantiation of RobEn paired with              els fall under one of two categories: (i) adversar-
    BERT achieves an average robust accuracy of               ial training, which augments the training set with
    71.3% against all adversarial typos in the fam-           heuristically generated perturbations and (ii) cer-
    ily considered, while previous work using a               tifiably robust training, which bounds the change
    typo-corrector achieves only 35.3% accuracy               in prediction between an input and any of its al-
    against a simple greedy attack.                           lowable perturbations. Both these approaches have
1   Introduction                                              major shortcomings, especially in NLP. Adversar-
                                                              ial training, while quite successful in vision (Madry
State-of-the-art NLP systems are brittle: small per-          et al., 2018), is challenging in NLP due to the
turbations of inputs, commonly referred to as ad-             discrete nature of textual inputs (Ebrahimi et al.,
versarial examples, can lead to catastrophic model            2018b); current techniques like projected gradi-
failures (Belinkov and Bisk, 2018; Ebrahimi et al.,           ent descent are incompatible with subword tok-
2018b; Ribeiro et al., 2018; Alzantot et al., 2018).          enization. Further, adversarial training relies on
For example, carefully chosen typos and word sub-             heuristic approximations to the worst-case pertur-
stitutions have fooled systems for hate speech de-            bations, leaving models vulnerable to new, stronger
tection (Hosseini et al., 2017), machine translation          attacks. Certifiably robust training (Jia et al., 2019;
    ∗
    Authors contributed equally.                              Huang et al., 2019; Shi et al., 2020) circumvents

                                                         2752
        Proceedings of the 58th Annual Meeting of the Association for Computational Linguistics, pages 2752–2765
                           July 5 - 10, 2020. c 2020 Association for Computational Linguistics
the above challenges by optimizing over a convex                            Input x
outer-approximation of the set of perturbations, al-          This     delightful       film    …   BERT    Pos
lowing us to lower bound the true robust accuracy.                   Perturbation set
However, the quality of bounds obtained by these              This      dlightful        fulm
                                                              Thus      deliightful      fllm
methods scale poorly with the size of the network,            …         …                …
and are vacuous for state-of-the-art models like              Tihs      delirhtful       fim
BERT. Moreover, both approaches require sepa-                                  x
                                                                       Perturbation x
rate, expensive training for each task, even when             Tihs     dlightful    fllm        …   BERT    Neg
defending against the same type of perturbations.
                                                          Figure 2: Attack model allowing independent perturba-
   Ideally we would like a “robustness” module
                                                          tions of each token. The original input, x is classified
that we can reuse across multiple tasks, allowing         by the model as positive while the perturbation x̃ =,
us to only worry about robustness once: during its        obtained by choosing perturbations of “This”, “delight-
construction. Indeed, reusable components have            ful”, and “film” independently, is classified as negative.
driven recent progress in NLP. For example, word          Independent perturbations of each word results in an
vectors are a universal resource that are constructed     exponentially large perturbation space B(x).
once, then used for many different tasks. Can
we build a reusable robust defense that can easily
                                                          work (Pruthi et al., 2019; Huang et al., 2019). We
work with complex, state-of-the-art architectures
                                                          consider a natural class of token-level encodings,
like BERT? The recent work of Pruthi et al. (2019),
                                                          which are obtained by encoding each token in a
which uses a typo-corrector to defend against adver-
                                                          sentence independently. This structure allows us to
sarial typos, is such a reusable defense: it is trained
                                                          express stability and fidelity in terms of a clustering
once, then reused across different tasks. However,
                                                          objective, which we optimize.
we find that current typo-correctors do not perform
                                                             Empirically, our instantiation of RobEn achieves
well against even heuristic attacks, limiting their
                                                          state-of-the-art robust accuracy, which we com-
applicability.
                                                          pute exactly, across six classification tasks from
   Our primary contribution is robust encodings           the GLUE benchmark (Wang et al., 2019). Our
(RobEn), a framework to construct encodings that          best system, which combines RobEn with a BERT
can make systems using any model robust. The              classifier (Devlin et al., 2019), achieves an average
core component of RobEn is an encoding function           robust accuracy of 71.3% across the six tasks. In
that maps sentences to a smaller discrete space           contrast, a state-of-the-art defense that combines
of encodings, which are then used to make pre-            BERT with a typo corrector (Pruthi et al., 2019)
dictions. We define two desiderata that a robust          gets 35.3% accuracy when adversarial typos are
encoding function should satisfy: stability and fi-       inserted, and a standard data augmentation defense
delity. First, to encourage consistent predictions        gets only 12.2% accuracy.
across perturbations, the encoding function should
map all perturbations of a sentence to a small set        2    Setup
of encodings (stability). Simultaneously, encod-
ings should remain expressive, so models trained          Tasks. We consider NLP tasks that require clas-
using encodings still perform well on unperturbed         sifying textual input x ∈ X to a class y ∈ Y. For
inputs (fidelity). Because systems using RobEn            simplicity, we refer to inputs as sentences. Each
are encoding-based we can compute the exact ro-           sentence x consists of tokens x1 , . . . , xL from the
bust accuracy tractably, avoiding the lower bounds        set of all strings T . Let ptask denote the distribution
of certifiably robust training. Moreover, these en-       over inputs and labels for a particular task of inter-
codings can make any downstream model robust,             est. The goal is to learn a model f : X → Y that
including state-of-the-art transformers like BERT,        maps sentences to labels, given training examples
and can be reused across different tasks.                 (x, y) ∼ ptask .

   In Section 4, we apply RobEn to combat adver-          Attack surface. We consider an attack surface in
sarial typos. In particular, we allow an attacker to      which an adversary can perturb each token xi of a
add independent edit distance one typos to each           sentence to some token x̃i ∈ B(xi ), where B(xi )
word in an input sentence, resulting in exponen-          is the set of valid perturbations of xi . For example,
tially more possible perturbations than previous          B(xi ) could be a set of allowed typos of xi . We

                                                     2753
define B(x) as the set of all valid perturbations             3.1      Encoding functions
of the set x, where every possible combination of
                                                              A RobEn classifier fα : X → Y using RobEn de-
token-level typos is allowed:
                                                              composes into two components: a fixed encoding
                                                              function α : X → Z, and a model that accepts en-
     B(x) = {(x˜1 , . . . , x˜L ) | x̃i ∈ B(xi ) ∀ i}   (1)
                                                              codings g : Z → Y.1 For any sentence x, our sys-
The size of the attack surface |B(x)| grows expo-             tem makes the prediction fα (x) = g(α(x)). Given
nentially with respect to number of input tokens, as          training data {(xi , yi )}ni=1 and the encoding func-
shown in Figure 2. In general xi ∈ B(xi ), so some            tion α, we learn g by performing standard training
words could remain unperturbed.                               on encoded training points {(α(xi ), yi )}ni=1 . To
                                                              compute the robust accuracy of this system, we
Model evaluation. In this work, we use three                  note that for well-chosen α and an input x from
evaluation metrics for any given task.                        some distribution Px , the set of possible encodings
  First, we evaluate a model on its standard accu-            α(x̃) for some perturbation x̃ ∈ B(x) is both small
racy on the task:                                             and tractable to compute quickly. We can thus com-
                                                              pute accrob (fα ) quickly by generating this set of
         accstd (f ) = E(x,y)∼ptask 1[f (x) = y].       (2)
                                                              possible encodings, and feeding each into g, which
   Next, we are interested in models that also have           can be any architecture.
high robust accuracy, the fraction of examples
                                                              3.2      Encoding function desiderata
(x, y) for which the model is correct on all valid per-
turbations x̃ ∈ B(x) allowed in the attack model:             In order to achieve high robust accuracy, a classifier
                                                              fα that uses α should make consistent predictions
     accrob (f ) = E(x,y)∼ptask min 1 [f (x̃) = y] .          on all x̃ ∈ B(x), the set of points described by the
                                x̃∈B(x)
                                                              attack surface, and also have high standard accu-
                                                        (3)
                                                              racy on unperturbed inputs. We term the former
It is common to instead compute accuracy against              property stability, and the latter fidelity, give intu-
a heuristic attack a that maps clean sentences x to           ition for both in this section, and provide a formal
perturbed sentences a(x) ∈ B(x).                              instantiation in Section 4.

    accattack (f ; a) = E(x,y)∼ptask 1[f (a(x)) = y]. (4)     Stability. For an encoding function α and some
                                                              distribution over inputs Px , the stability Stab(α)
Typically, a(x) is the result of a heuristic search for       measures how often α maps sentences x ∼ Px to
a perturbation x̃ ∈ B(x) that f misclassifies. Note           the same encoding as all of their perturbations.
that accattack is a (possibly loose) upper bound of
accrob because there could be perturbations that the          Fidelity. An encoding function α has high fi-
model misclassifies but are not encountered during            delity if models that use α can still achieve high
the heuristic search (Athalye et al., 2018).                  standard accuracy. Unfortunately, while we want to
                                                              make task agnostic encoding functions, standard ac-
    Additionally, since robust accuracy is generally
                                                              curacy is inherently task dependent: different tasks
hard to compute, some existing work computes cer-
                                                              have different expected distributions over inputs
tified accuracy (Huang et al., 2019; Jia et al., 2019;
                                                              and labels. To emphasize this challenge consider
Shi et al., 2020), which is a potentially conserva-
                                                              two tasks: for an integer n, predict n mod 2, and
tive lower bound for the true robust accuracy. In
                                                              n mod 3. The information we need encodings to
this work, since we use robust encodings, we can
                                                              preserve varies significantly between these tasks:
tractably compute the exact robust accuracy.
                                                              for the former, 2 and 6 can be identically encoded,
3     Robust Encodings                                        while for the latter they must encoded separately.
                                                                 To overcome this challenge, we consider a sin-
We introduce robust encodings (RobEn), a frame-               gle distribution over the inputs Px that we believe
work for constructing encodings that are reusable             covers many task-distributions ptask . Since it is
across many tasks, and pair with arbitrary model              hard to model the distribution over the labels, we
architectures. In Section 3.1 we describe the key             take the more conservative approach of mapping
components of RobEn, then in Section 3.2 we high-
                                                                 1
light desiderata RobEn should satisfy.                               We can set Z ⊆ X when g accepts sentences.

                                                          2754
the different sentences sampled from Px to differ-
ent encodings with high probability. We call this                          aunt             abrupt
Fid(α), and give an example in Section 4.5.                       aut
                                                                              auet                   aboupt
Tradeoff. Stability and fidelity are inherently                 at                                about
competing goals. An encoding function that maps
                                                                     aet     abet         abot

every sentence to the same encoding trivially max-
imizes stability, but is useless for any non-trivial                          Maximal stability
                                                                                 Balanced
classification task. Conversely, fidelity is maxi-
                                                                              Maximal fidelity
mized when every input is mapped to itself, which
has very low stability. In the following section,
                                                        Figure 3: Visualization of three different encodings.
we construct an instantiation of RobEn that bal-        Vocabulary words (large font, blue) share an edge if
ances stability and fidelity when the attack surface    they share a common perturbation (small font, red).
consists of typos.                                      The maximal stability cluster (thick solid line) clusters
                                                        identically, the maximal fidelity clusters (thin dotted
4     Robust Encodings for Typos                        line) encodes all words separately, while the balanced
                                                        clusters (thin solid line) trade off the two.
In this section, we focus on adversarial typos,
where an adversary can add typos to each token
in a sentence (see Figure 2). Since this attack sur-       Since the space of possible tokens T is in-
face is defined at the level of tokens, we restrict     numerable, we focus on a smaller set of words
attention to encoding functions that encode each        V = {w1 , . . . , wN } ⊆ T , which contains the N
token independently. Such an encoding does not          most frequent words over Px . We will call elements
use contextual information; we find that even such      of V words, and tokens that are perturbations of
robust encodings achieve greater attack accuracy        some word typos. We view deciding which words
and robust accuracy in practice than previous work.     should share an encoded token as assigning words
   First, we will reduce the problem of generat-        to clusters C1 , . . . , Ck ⊆ V . For all other tokens
ing token level encodings to assigning vocabulary       not in the vocabulary, including typos, we define a
words to clusters (Section 4.1). Next, we use an ex-    separate πOOV . Thus, we decompose π as follows:
ample to motivate different clustering approaches                             (
                                                                                 πV (xi )   xi ∈ V
(Section 4.2), then describe how we handle out-of-                π(xi ) =                           ,     (6)
vocabulary tokens (Section 4.3). Finally, we in-                                 πOOV (xi ) xi ∈
                                                                                               /V
troduce two types of token-level robust encodings:      Here, πV is associated with a clustering C of vo-
connected component encodings (Section 4.4) and         cabulary words, where each cluster is associated
agglomerative cluster encodings (Section 4.5).          with a unique encoded token.
4.1    Encodings as clusters                            4.2   Simple example
We construct an encoding function α that encodes        We use a simple example to illustrate how a token-
x token-wise. Formally, α is defined by a token-        level encoding function can achieve the RobEn
level encoding function π that maps each token          desiderata: stability and fidelity defined in Section
xi ∈ T to some encoded token π(xi ) ∈ ZTok :            3.2. We will formally define the stability and fi-
         α(x) = [π(x1 ), π(x2 ), . . . π(xL )].   (5)   delity of a clustering in Sections 4.3 and 4.5.
                                                           Consider the five words (large font, blue) in Fig-
In the RobEn pipeline, a downstream model g is          ure 3, along with potential typos (small font, red).
trained on encodings (Section 3.1). If π maps many      We illustrate three different clusterings as boxes
words and their typos to the same encoded token,        around tokens in the same cluster. We may put all
they become indistinguishable to g, conferring ro-      words in the same cluster (thick box), each word
bustness. In principle, the relationship between dif-   in its own cluster (dashed boxes), or something
ferent encoded tokens is irrelevant: during training,   in between (thin solid boxes). For now, we group
g learns how to use the encoded tokens to perform       each typo with a word it could have been perturbed
a desired task. Thus, the problem of finding a good     from (we will discuss this further in Section 4.3).
π is equivalent to deciding which tokens should            To maximize stability, we need to place all words
share the same encoded token.                           in the same cluster. Otherwise, there would be two

                                                    2755
 words (say “at” and “aunt”) that could both be         Finally, when a token is not a typo of any vocab
 perturbed to the same typo (“aut”) but are in dif-     words, we encode it to a special token OOV.
 ferent clusters. Therefore, “aut” cannot map to
 the same encoded token as both the possible vocab      4.4   Connected component encodings
 words. At the other extreme, to maximize fidelity,     We present two approaches to generate robust
 each word should be in its own cluster. Both map-      token-level encodings. Our first method, connected
 pings have weaknesses: the stability-maximizing        component encodings, maximizes the stability ob-
 mapping has low fidelity since all words are iden-     jective (8). Notice that Stab is maximized when for
 tically encoded and thus indistinguishable, while      each word w, Bπ (w) contains one encoded token.
 the fidelity-maximizing mapping has low stabil-        This is possible only when all words that share a
 ity since the typos of words “aunt”, “abet”, and       typo are assigned to the same cluster.
“abrupt” could all be mapped to different encoded          To maximize Stab, define a graph G with all
 tokens than that of the original word.                 words in V as vertices, and edges between words
    The clustering represented by the thin solid        that share a typo. Since we must map words that
 boxes in Figure 3 balances stability and fidelity.     share an edge in G to the same cluster, we define
 Compared to encoding all words identically, it has     the cluster Ci to be the set of words in the ith
 higher fidelity, since it distinguishes between some   connected component of G. While this stability-
 of the words (e.g., “at” and “about” are encoded       maximizing clustering encodes many words to the
 differently). It also has reasonably high stability,   same token (and hence seems to compromise on
 since only the infrequent “abet” has typos that        fidelity), these encodings still perform surprisingly
 are shared across words and hence are mapped to        well in practice (see Section 5.4).
 different encoded tokens.
                                                        4.5   Agglomerative cluster encodings
 4.3   Encoding out-of-vocab tokens
                                                      Connected component encodings focus only sta-
 Given a fixed clustering of V , we now study how     bility and can lead to needlessly low fidelity. For
 to map out-of-vocabulary tokens, including typos,    example, in Figure 3, “at” and “about” are in the
 to encoded tokens without compromising stability.    same connected component even though they don’t
Stability. Stability measures the extent to which     share a typo. Since both words are generally fre-
typos of words map to different encoded tokens. quent, mapping them to different encoded tokens
We formalize this by defining the set of tokens that  can significantly improve fidelity, with only a small
some typo of a word w could map to, Bπ (w):           drop in stability: recall only the infrequent word
                                                     “abet” can be perturbed to multiple encoded tokens.
          Bπ (w) = {π(w̃); w̃ ∈ B(w)},           (7)     To handle such cases, we introduce agglomera-
                                                      tive cluster encodings, which we construct by trad-
where B(w) is the set of allowable typos of w. ing off Stab with a formal objective we define for
Since we care about inputs drawn from Px , we         fidelity: Fid. We then approximately optimize this
define Stab on the clustering C using ρ(w), the       combined objective Φ using an agglomerative clus-
normalized frequency of word w based on Px .          tering algorithm.
                        N
                        X                               Fidelity objective. Recall from Section 3.2 that
         Stab(C) = −          ρ(wi )|Bπ (wi )|   (8)
                                                        an encoding has high fidelity if it can be used to
                        i=1
                                                        achieve high standard accuracy on many tasks. This
For a fixed clustering, the size of Bπ (w) depends      is hard to precisely characterize: we aim to design
on where πOOV maps typos that w shares with other       an objective that could approximate this.
words; for example in Figure 3, “aet” could be a           We note that distinct encoded tokens are arbi-
perturbation of both “at” and “abet”. If we map         trarily related: the model g learns how to use dif-
the typo the encoded token of “at”, we increase         ferent encodings during training. Returning to our
the size of Bπ (”abet”) and vice-versa. In order to     example, suppose “at” and “abet” belong to the
keep the size of Bπ (w) smaller for the more fre-       same cluster and share an encoded token z. Dur-
quent words and maximize stability (Equation 8),        ing training, each occurrence of “at” and “abet”
we map a typo to the same encoded token as its          is replaced with z. However, since “at” is much
most frequent neighbor word (in this case “at”).        more frequent, classifiers treat z similarly to “at 00

                                                   2756
in order to achieve good overall performance. This       For every word in the input, the adversary is al-
leads to mostly uncompromised performance on             lowed to insert a lowercase letter, delete a charac-
sentences with “at”, at the cost of performance on       ter, substitute a character for any letter, or swap
sentences containing the less frequent “abet”.           two adjacent characters, so long as the first and last
   This motivates the following definition: let ~vi      characters remain the same as in the original token.
be a the indicator vector in R|V | corresponding to      The constraint on the outer characters, also used by
word i. In principle ~vi could be a word embedding;      Pruthi et al. (2019), is motivated by psycholinguis-
we choose indicator vectors to avoid making addi-        tic studies (Rawlinson, 1976; Davis, 2003).
tional assumptions. We define the encoded token             Within our attack surface, “the movie was miser-
µ~j associated with words in cluster Cj as follows:      able” can be perturbed to “thae mvie wjs misreable”
                     P                                   but not “th movie as miserable”. Since each to-
                         w ∈C ρ(wi )~ vi
              µ~j = P i j                       (9)      ken can be independently perturbed, the number
                          wi ∈Cj ρ(wi )                  of perturbations of a sentence grows exponentially
We weight by the frequency ρ to capture the effect       with its length; even “the movie was miserable”
of training on the encodings, as described above.        has 431,842,320 possible perturbations. Our attack
   Fidelity is maximized when each word has a            surface contains the attack surface used by (Pruthi
distinct encoded token. We capture the drop in           et al., 2019), which allows ED1 perturbations to at
standard accuracy due to shared encoded tokens by        most two words per sentence. Reviews from SST-2
computing the distance between the original em-          have 5 million perturbations per example (PPE) on
beddings of the word its encoded token. Formally,        average under this attack surface, while our attack
let c(i) be the cluster index of word wi . We define     surface averages 1097 PPE. We view the size of
the fidelity objective Fid as follows:                   the attack surface as a strength of our approach:
                   N
                                                         our attack surface forces a system robust to subtle
                                                         perturbations (“the moviie waas misreable”) that
                   X
      Fid(C) = −                     µc(i) k2 .
                                vi − ~
                         ρ(wi )k~                 (10)
                   i=1                                   smaller attack surfaces miss.
                                                            In Section 5.7, we additionally consider the in-
Fid is high if frequent words and rare words are
                                                         ternal permutation attacks studied in Belinkov and
in the same cluster and is low when when multiple
                                                         Bisk (2018) and Sakaguchi et al. (2017), where
frequent words are in the same cluster.
                                                         all characters, except the first and the last, may be
Final objective. We introduce a hyperparameter           arbitrarily reordered.
γ ∈ [0, 1] that balances stability and fidelity. We
approximately minimize the following weighted            Attack algorithms. We consider two attack algo-
combination of Stab (8) and Fid (10):                    rithms: the worst-case attack (WCA) and a beam-
                                                         search attack (BSA). WCA exhaustively tests ev-
      Φ(C) = γ Fid(C) + (1 − γ) Stab(C).          (11)   ery possible perturbation of an input x to see any
As γ approaches 0, we get the connected compo-           change in the prediction. The attack accuracy of
nent clusters from our baseline, which maximize          WCA is the true robust accuracy since if there ex-
stability. As γ approaches 1, we maximize fidelity       ists some perturbation that changes the prediction,
by assigning each word to its own cluster.               WCA finds it. When instances of RobEn have high
                                                         stability, the number of possible encodings of per-
Agglomerative clustering. We approximate the             turbations of x is often small, allowing us to exhaus-
optimal value of Φ using agglomerative clustering;       tively test all possible perturbations in the encoding
we start with each word in its own cluster, then iter-   space.2 This allows us to tractably run WCA. Using
atively combine the pair of clusters whose resulting     WCA with RobEn, we can obtain computationally
combination increases Φ the most. We repeat until        tractable guarantees on robustness: given a sen-
combining any pair of clusters would decrease Φ.         tence, we can quickly compute whether or not any
Further details are provided in Appendix A.1.            perturbation of x that changes the prediction.
5     Experiments                                           For systems that don’t use RobEn, we cannot
                                                         tractably run WCA. Instead, we run a beam search
5.1     Setup
                                                            2
                                                              When there are more than 10000 possible encodings,
Token-level attacks. The primary attack surface          which holds for 0.009% of our test examples, we assume
we study is edit distance one (ED1) perturbations.       the adversary successfully alters the prediction.

                                                     2757
attack (BSA) with beam width 5, perturbing tokens         particular, we train a scRNN typo-corrector (Sak-
one at a time. For efficiency, we sample at most          aguchi et al., 2017) on random perturbations of
len(xi ) perturbations at each step of the search (see    each task’s training set. At test time inputs are
Apendix A.2). Even against this very limited attack,     “corrected” using the typo corrector, then fed into
we find that baseline models have low accuracy.           a downstream model. We replace any OOV out-
                                                          putted by the typo-corrector with the neutral word
Datasets. We use six of the nine tasks from
                                                         “a” and use BERT as our downstream model.
GLUE (Wang et al., 2019): SST-2, MRPC, QQP,
MNLI, QNLI, and RTE. We do not use STS-B and             5.3   Models with RobEn
CoLA as they are evaluated on correlation, which
                                                         We run experiments using our two token-level
does not decompose as an example-level loss. We
                                                         encodings: connected component encodings
additionally do not use WNLI, as most submitted
                                                         (C ONN C OMP) and agglomerative cluster encod-
GLUE models cannot even outperform the major-
                                                         ings (AGG C LUST). To form clusters, we use the
ity baseline, and state-of-the-art models are rely on
                                                         N = 100, 000 most frequent words from the Cor-
external training data (Kocijan et al., 2019). We
                                                         pus of Contemporary American English (Davies,
evaluate on the test sets for SST-2 and MRPC, and
                                                         2008) that are also in GloVe (Pennington et al.,
the publicly available dev sets for the remaining
                                                         2014). For AGG C LUST we use γ = 0.3, which
tasks. More details are provided in Appendix A.3.
                                                         maximizes robust accuracy on SST-2 dev set.
5.2   Baseline models.
                                                         Form of encodings. Though unnecessary when
We consider three baseline systems. Our first is the     training from scratch, to leverage the inductive bi-
standard base uncased BERT model (Devlin et al.,         ases of pre-trained models like BERT (Devlin et al.,
2019) fine-tuned on the training data for each task.3    2019), we define the encoded token of a cluster to
                                                         be the cluster’s most frequent member word. In
Data augmentation. For our next baseline, we
                                                         the special case of the out-of-vocab token, we map
augment the training dataset with four random per-
                                                         OOV to [MASK]. Our final encoding, α(x), is the
turbations of each example, then fine-tune BERT
                                                         concatenation of all of these words. For both encod-
on this augmented data. Data augmentation has
                                                         ings, we fine-tune BERT on the training data, using
been shown to increase robustness to some types of
                                                         α(x) as input. Further details are in Appendix A.4.
adversarial perturbations (Ribeiro et al., 2018; Liu
et al., 2019). Other natural baselines all have severe   5.4   Robustness gains from RobEn
limitations. Adversarial training with black-box at-
tacks offers limited robustness gains over data aug-     Our main results are shown in Table 1. We show
mentation (Cohen et al., 2019; Pruthi et al., 2019).     all three baselines, as well as models using our
Projected gradient descent (Madry et al., 2017), the     instances of RobEn: C ONN C OMP and AGG C LUST.
only white-box adversarial training method that is          Even against the heuristic attack, each baseline
robust in practice, cannot currently be applied to       system suffers dramatic performance drops. The
BERT since subword tokenization maps different           system presented by Pruthi et al. (2019), Typo Cor-
perturbations to different numbers of tokens, mak-       rector + BERT, only achieves 35.3% attack accu-
ing gradient-based search impossible. Certifiably        racy, compared to its standard accuracy of 78.2%.
robust training (Huang et al., 2019; Shi et al., 2020)   BERT and Data Augmentation + BERT perform
does not work with BERT due to the same tokeniza-        even worse. Moreover, the number of perturbations
tion issue and BERT’s use of non-monotonic acti-         the heuristic attack explores is a tiny fraction of our
vation functions, which make computing bounds            attack surface, so the robust accuracy of Typo Cor-
intractable. Moreover the bounds computed with           rector + BERT, the quantity we’d like to measure,
certifiably robust training, which give guarantees,      is likely far lower than the attack accuracy.
become loose as model depth increases, hurting              In contrast, simple instances of RobEn are much
robust performance (Gowal et al., 2018).                 more robust. AGG C LUST + BERT achieves av-
                                                         erage robust accuracy of 71.3%, 36 points higher
Typo-corrector. For our third baseline, we use           than the attack accuracy of Typo Corrector + BERT.
the most robust method from Pruthi et al. (2019). In     AGG C LUST also further improves on C ONN C OMP
  3
    https://github.com/huggingface/                      in terms of both robust accuracy (by 1.3 points)
pytorch-transformers                                     and standard accuracy (by 2.8 points).

                                                     2758
            Accuracy         System          SST-2     MRPC      QQP                          MNLI        QNLI     RTE    Avg
                       Baselines
                       BERT                   93.8       87.7    91.3                              84.6   88.6     71.1    86.2
                       Data Aug. + BERT       92.2       84.3    88.7                              83.0   87.4     63.5    83.1
            Standard   Typo Corr. + BERT      89.6       80.9    87.6                              75.9   80.5     54.9    78.2
                       RobEn
                       Con. Comp. + BERT      80.6       79.9    84.2                              65.7   73.3     52.7    72.7
                       Agg. Clust. + BERT     83.1       83.8    85.0                              69.1   76.6     59.2    76.1
                       Baselines
                       BERT                    8.7       10.0    17.4                               0.7    0.7      1.8     6.6
                       Data Aug. + BERT       17.1        1.0    27.6                              15.4   10.7      1.4    12.2
            Attack     Typo Corr. + BERT      53.2       30.1    52.0                              23.0   32.3     21.3    35.3
                       RobEn
                       Con. Comp. + BERT      80.3       79.4    82.7                           62.6      71.5     47.3   70.6
                       Agg. Clust. + BERT     82.1       82.8    83.2                           65.3      74.5     52.7   73.4
                       RobEn
            Robust     Con. Comp. + BERT      80.1       79.4    82.2                           61.4      70.5     46.6   70.0
                       Agg. Clust. + BERT     80.7       80.9    81.4                           62.8      71.9     49.8   71.3

Table 1: Standard, attack, and robust accuracy on six GLUE tasks against ED1 perturbations. For baseline models
we only compute attack accuracy, an upper bound on robust accuracy, since robust accuracy cannot be tractably
computed. Using RobEn, we get robustness guarantees by computing robust accuracy, which we find outperforms
a the typo corrector in (Pruthi et al., 2019) by at least 36 points.


Standard accuracy. Like defenses against adver-                                               Size of Bα on SST-2 Size of Bα on RTE
                                                                                        1.0
                                                                                                                          C ONN C OMP




                                                                 Fraction of Examples
sarial examples in other domains, using RobEn
                                                                                        0.8                  0.8          AGG C LUST
decreases standard accuracy (Madry et al., 2017;
                                                                                        0.6                  0.6
Zhang et al., 2019; Jia et al., 2019). Our agglomer-
ative cluster encodings’s standard accuracy is 10.1                                     0.4                  0.4
points lower then that of normally trained BERT.                                        0.2                  0.2
However, to the best of our knowledge, our stan-                                         0
dard accuracy is state-of-the-art for approaches that                                          1     2 3–4 5–8 9+ 1 2 3–4 5–8 9+
                                                                                                              Size of Bα
guarantee robustness. We attribute this improve-
ment to RobEn’s compatibility with any model.              Figure 4: Histogram of |Bα (x)| for SST-2 and RTE.
                                                           SST-2 has the highest percentage of inputs x where
Comparison to smaller attack surfaces. We                  |Bα (x)| = 1, while RTE has the least. On both
note that RobEn also outperform existing methods           datasets, |Bα (x)| < 9 for most x, and |Bα (x)| = 1
on their original, smaller attack surfaces. On SST-2,      on a plurality of inputs.
Pruthi et al. (2019) achieves an accuracy of 75.0%
defending against a single ED1 typo, which is 5.7
points lower than AGG C LUST’s robust accuracy             function. Figure 4 plots the distribution of |Bα (x)|,
against perturbations of all tokens: a superset of the     across test examples in SST-2 and RTE, where
original perturbation set. We discuss constrained          Bα (x) is the set of encodings that are mapped to by
adversaries further in Appendix A.5. AGG C LUST            some perturbation of x. Over AGG C LUST encod-
also outperforms certified training: Huang et al.          ings, |Bα (x)| = 1 for 25% of examples in RTE and
(2019), which offers robustness guarantees to three        66% in SST-2, with the other four datasets falling
character substitution typos (but not insertions or        between these extremes (see Appendix A.6). As
deletions), achieves a robust accuracy of 74.9%            expected, these numbers are even higher for the
on SST-2. Certified training requires strong as-           connected component encodings. Note that when
sumptions on model architecture; even the robust           |Bα (x)| = 1, every perturbation of x maps to the
accuracy of AGG C LUST outperforms the standard            same encoding. When |Bα (x)| is small, robust
accuracy of the CNN used in Huang et al. (2019).           accuracy can be computed quickly.

5.5   Reusable encodings                                   5.6      Agglomerative Clustering Tradeoff
Each instance of RobEn achieves consistently high          In Figure 5, we plot standard and robust accuracy
stability across our tasks, despite reusing a single       on SST-2 for AGG C LUST encodings, using differ-

                                                     2759
                         SST-2 Standard and Robust Accuracies       corrector to correct typos chosen to create ambi-
                 0.9                                                guity as to the original word, but these typos are
                                                                    not adversarially chosen to fool a model. Edizel

      Accuracy
                 0.8
                                                                    et al. (2019) attempt to learn typo-resistant word
                                                                    embeddings, but focus on common typos, rather
                                                                    than worst-case typos. In computer vision, Chen
                 0.7
                              Standard accuracy                     et al. (2019) discretizes pixels to compute exact
                              Robust accuracy
                                                                    robust accuracy on MNIST, but their approach gen-
                   0.0           0.2           0.4         0.6
                              Fidelity Objective Weight (γ)
                                                                    eralizes poorly to other tasks like CIFAR-10. Garg
                                                                    et al. (2018) generate functions that map to robust
Figure 5: Standard and robust accuracies on SST-2 with              features, while enforcing variation in outputs.
AGG C LUST using different values of γ. While the gap
between standard and robust accuracy increases mono-                Incorporating context. Our token-level robust
tonically, robust accuracy increases before decreasing.             encodings lead to strong performance, despite ig-
                                                                    noring useful contextual information. Using con-
                                                                    text is not fundamentally at odds with the idea of
ent values of γ. Recall that γ = 0 maximizes                        robust encodings, and making contextual encod-
stability (C ONN C OMP), and γ = 1 maximizes fi-                    ings stable is an interesting technical challenge and
delity. At γ = 0, the gap between standard and                      a promising direction for future work.
robust accuracy, due to out-of-vocabulary tokens,                      In principle, an oracle that maps every word with
is negligible. As γ increases, both standard ac-                    a typo to the correct unperturbed word seems to
curacy and the gap between standard and robust                      have higher fidelity than our encodings, without
accuracy increase. As a result, robust accuracy first               compromising stability. However, existing typo
increases, then decreases.                                          correctors are far from perfect, and a choosing an
                                                                    incorrect unperturbed word from a perturbed input
5.7    Internal permutation attacks
                                                                    leads to errors in predictions of the downstream
RobEn can also be used to defend against the in-                    model. This mandates an intractable search over
ternal perturbations described in Section 5.1. For                  all perturbations to compute the robust accuracy.
normally trained BERT, a heuristic beam search
attack using internal permutations reduces aver-                    Task-agnosticity. Many recent advances in NLP
age accuracy from 86.2% to 15.7% across our six                     have been fueled by the rise of task-agnostic rep-
tasks. Using C ONN C OMP with the internal permu-                   resentations, such as BERT, that facilitate the cre-
tation attack surface, we achieve robust accuracy                   ation of accurate models for many tasks. Robust-
of 81.4%. See Appendix A.7 for further details.                     ness to typos should similarly be achieved in a
                                                                    task-agnostic manner, as it is a shared goal across
6     Discussion                                                    many NLP tasks. Our work shows that even simple
                                                                    robust encodings generalize across tasks and are
Additional related work. In this work, we intro-                    more robust than existing defenses. We hope our
duce RobEn, a framework to construct systems that                   work inspires new task-agnostic robust encodings
are robust to adversarial perturbations. We then use                that lead to more robust and more accurate models.
RobEn to achieve state-of-the-art robust accuracy
when defending against adversarial typos. Besides                   Acknowledgments
typos, other perturbations can also be applied to                   This work was supported by NSF Award Grant no.
text. Prior attacks consider semantic operations,                   1805310 and the DARPA ASED program under
such as replacing a word with a synonym (Alzantot                   FA8650-18-2-7882. A.R. is supported by a Google
et al., 2018; Ribeiro et al., 2018). Our framework                  PhD Fellowship and the Open Philanthropy Project
extends easily to these perturbations. Other attack                 AI Fellowship. We thank Pang Wei Koh, Reid
surfaces involving insertion of sentences (Jia and                  Pryzant, Ethan Chi, Daniel Kang, and the anony-
Liang, 2017) or syntactic rearrangements (Iyyer                     mous reviewers for their helpful comments.
et al., 2018) are harder to pair with RobEn, and are
interesting directions for future work.                             Reproducibility
   Other defenses are based on various forms of                     All code, data, and experiments are available on
preprocessing. Gong et al. (2019) apply a spell-                    CodaLab at https://bit.ly/2VSZI2e.

                                                                 2760
References                                                  H. Gong, Y. Li, S. Bhat, and P. Viswanath. 2019.
                                                              Context-sensitive malicious spelling error correc-
M. Alzantot, Y. Sharma, A. Elgohary, B. Ho, M. Srivas-        tion. In World Wide Web (WWW), pages 2771–2777.
  tava, and K. Chang. 2018. Generating natural lan-
  guage adversarial examples. In Empirical Methods          S. Gowal, K. Dvijotham, R. Stanforth, R. Bunel,
  in Natural Language Processing (EMNLP).                     C. Qin, J. Uesato, T. Mann, and P. Kohli. 2018. On
                                                              the effectiveness of interval bound propagation for
A. Athalye, N. Carlini, and D. Wagner. 2018. Obfus-           training verifiably robust models. arXiv preprint
  cated gradients give a false sense of security: Cir-        arXiv:1810.12715.
  cumventing defenses to adversarial examples. arXiv
  preprint arXiv:1802.00420.                                H. Hosseini, S. Kannan, B. Zhang, and R. Pooven-
                                                              dran. 2017. Deceiving Google’s Perspective API
Y. Belinkov and Y. Bisk. 2018. Synthetic and natu-
                                                              built for detecting toxic comments. arXiv preprint
   ral noise both break neural machine translation. In
                                                              arXiv:1702.08138.
   International Conference on Learning Representa-
   tions (ICLR).                                            P. Huang, R. Stanforth, J. Welbl, C. Dyer, D. Yo-
                                                               gatama, S. Gowal, K. Dvijotham, and P. Kohli. 2019.
J. Chen, X. Wu, V. Rastogi, Y. Liang, and S. Jha.
                                                               Achieving verified robustness to symbol substitu-
   2019. Towards understanding limitations of pixel
                                                               tions via interval bound propagation. In Empirical
   discretization against adversarial attacks. In IEEE
                                                               Methods in Natural Language Processing (EMNLP).
   European Symposium on Security and Privacy (Eu-
   roS&P).                                                  M. Iyyer, J. Wieting, K. Gimpel, and L. Zettlemoyer.
J. M. Cohen, E. Rosenfeld, and J. Z. Kolter. 2019. Cer-       2018. Adversarial example generation with syntac-
   tified adversarial robustness via randomized smooth-       tically controlled paraphrase networks. In North
   ing. In International Conference on Machine Learn-         American Association for Computational Linguistics
   ing (ICML).                                                (NAACL).

M. Davies. 2008. The corpus of contemporary amer-           R. Jia and P. Liang. 2017. Adversarial examples for
  ican english (coca): 560 million words, 1990-               evaluating reading comprehension systems. In Em-
  present. https://www.english-corpora.org/                   pirical Methods in Natural Language Processing
  faq.asp.                                                    (EMNLP).

M. Davis. 2003. Psycholinguistic evidence on scram-         R. Jia, A. Raghunathan, K. Gksel, and P. Liang. 2019.
  bled letters in reading. https://www.mrc-cbu.                Certified robustness to adversarial word substitu-
  cam.ac.uk/.                                                  tions. In Empirical Methods in Natural Language
                                                              Processing (EMNLP).
J. Devlin, M. Chang, K. Lee, and K. Toutanova. 2019.
   BERT: Pre-training of deep bidirectional transform-      V. Kocijan, A. Cretu, O. Camburu, Y. Yordanov, and
   ers for language understanding. In Association              T. Lukasiewicz. 2019. A surprisingly robust trick
   for Computational Linguistics (ACL), pages 4171–            for the Winograd schema challenge. In Association
   4186.                                                       for Computational Linguistics (ACL).

W. B. Dolan and C. Brockett. 2005. Automatically con-       H. Lee and A. Y. Ng. 2005. Spam deobfuscation using
  structing a corpus of sentential paraphrases. In Inter-     a hidden Markov model. In Conference on Email
  national Workshop on Paraphrasing (IWP).                    and Anti-Spam (CEAS).

J. Ebrahimi, D. Lowd, and D. Dou. 2018a. On adver-          N. F. Liu, R. Schwartz, and N. A. Smith. 2019. Inocu-
   sarial examples for character-level neural machine         lation by fine-tuning: A method for analyzing chal-
   translation. In International Conference on Compu-         lenge datasets. In North American Association for
   tational Linguistics (COLING).                             Computational Linguistics (NAACL).

J. Ebrahimi, A. Rao, D. Lowd, and D. Dou. 2018b. Hot-       A. Madry, A. Makelov, L. Schmidt, D. Tsipras, and
   flip: White-box adversarial examples for text classi-      A. Vladu. 2017. Towards deep learning models
   fication. In Association for Computational Linguis-        resistant to adversarial attacks (published at ICLR
   tics (ACL).                                                2018). arXiv.

B. Edizel, A. Piktus, P. Bojanowski, R. Ferreira,           A. Madry, A. Makelov, L. Schmidt, D. Tsipras, and
  E. Grave, and F. Silvestri. 2019. Misspelling oblivi-       A. Vladu. 2018. Towards deep learning models re-
  ous word embeddings. In North American Associa-             sistant to adversarial attacks. In International Con-
  tion for Computational Linguistics (NAACL).                 ference on Learning Representations (ICLR).

S. Garg, V. Sharan, B. H. Zhang, and G. Valiant. 2018.      J. Pennington, R. Socher, and C. D. Manning. 2014.
   A spectral view of adversarially robust features. In        GloVe: Global vectors for word representation. In
   Advances in Neural Information Processing Systems           Empirical Methods in Natural Language Processing
   (NeurIPS).                                                  (EMNLP), pages 1532–1543.

                                                       2761
D. Pruthi, B. Dhingra, and Z. C. Lipton. 2019. Combat-    the graph from Section 4.2. For each such pair, we
   ing adversarial misspellings with robust word recog-   compute the value of Φ if Ci and Cj were replaced
   nition. In Association for Computational Linguistics
                                                          by Ci ∪ Cj . If no merge operation causes Φ to
  (ACL).
                                                          decrease, we return the current πV . Otherwise, we
P. Rajpurkar, J. Zhang, K. Lopyrev, and P. Liang. 2016.   merge the pair that leads to the greatest reduction
   SQuAD: 100,000+ questions for machine compre-          in Φ, and repeat. To merge two clusters Ci and
   hension of text. In Empirical Methods in Natural
   Language Processing (EMNLP).                           Cj , we first compute a new encoded token r as
                                                          the w ∈ Ci ∪ Cj with largest ρ(w). We then set
G. E. Rawlinson. 1976. The significance of letter posi-   πV (w) = r for all w ∈ Ci ∪ Cj . Our algorithm
  tion in word recognition. Ph.D. thesis, University of
                                                          thus works as follows
  Nottingham.
M. T. Ribeiro, S. Singh, and C. Guestrin. 2018. Se-       Algorithm 1 Objective-minimizing agglomerative
  mantically equivalent adversarial rules for debug-      clustering
  ging NLP models. In Association for Computational
                                                            1: C ← V
  Linguistics (ACL).
                                                            2: for i in range(|V |) do
K. Sakaguchi, K. Duh, M. Post, and B. V. Durme. 2017.       3:   Cnext ← Get Best Combination(C)
   Robsut wrod reocginiton via semi-character recur-
                                                            4:   if C = Cnext then
   rent neural network. In Association for the Advance-
   ment of Artificial Intelligence (AAAI).                  5:      return C
                                                            6:   end if
Z. Shi, H. Zhang, K. Chang, M. Huang, and C. Hsieh.         7:   C ← Cnext
   2020. Robustness verification for transformers. In
   International Conference on Learning Representa-         8: end for
   tions (ICLR).                                            9: return C

R. Socher, A. Perelygin, J. Y. Wu, J. Chuang, C. D.
  Manning, A. Y. Ng, and C. Potts. 2013. Recursive          Now, we simply have to define the procedure we
  deep models for semantic compositionality over a        use to get the best combination.
  sentiment treebank. In Empirical Methods in Nat-
  ural Language Processing (EMNLP).                       Algorithm 2 Get Best Combination(C)
A. Wang, A. Singh, J. Michael, F. Hill, O. Levy, and        1: Copt ← C
  S. R. Bowman. 2019. Glue: A multi-task bench-             2: Φopt ← Φ(C)
  mark and analysis platform for natural language un-
  derstanding. In International Conference on Learn-        3: for (Ci , Cj ) ∈ Adjacent Pairs(C) do
  ing Representations (ICLR).                               4:  Ccomb ← Ci ∪ Cj
                                                            5:  Cnew ← C ∪ Ccomb \ {Ci , Cj } {New clus-
A. Williams, N. Nangia, and S. Bowman. 2018. A
  broad-coverage challenge corpus for sentence under-
                                                                ters}
  standing through inference. In Association for Com-      6:   Φnew ← Φ(Cnew )
  putational Linguistics (ACL), pages 1112–1122.           7:   if Φnew < Φopt then
                                                           8:      Φopt ← Φnew
H. Zhang, Y. Yu, J. Jiao, E. P. Xing, L. E. Ghaoui, and
  M. I. Jordan. 2019. Theoretically principled trade-      9:      Copt ← Cnew
  off between robustness and accuracy. In Interna-        10:   end if
  tional Conference on Machine Learning (ICML).           11: end for
                                                          12: return Copt
A     Appendix
A.1    Aggloemrative clustering                              Recall our graph G = (G, E) used to define
Recall that any πV induces a clustering of V , where      the connected component clusters. We say two
each cluster contains a set of words mapped by πV         clusters Ci and Cj are adjacent, and thus returned
to the same encoded token. We use an agglomer-            by Adjacent Pairs, if there exists a vi ∈ Ci and a
ative clustering algorithm to approximately mini-         vj ∈ Cj such that (vi , vj ) ∈ GE . The runtime of
mize Φ. We initialize πV by setting πV (w) = w            our algorithm is O(N 2 E) since at each of a pos-
for each w ∈ V , which corresponds to placing             sible N total iterations, we compute the objective
each word in its own cluster. We then examine             for one of at most E pairs of clusters. Computation
each pair of clusters Ci , Cj such that there exists      of the objective can be reframed as computing the
an edge between a node in Ci and a node in Cj , in        difference between Φ and Φnew , where the latter is

                                                     2762
computed using new clusters, which can be done              training on 80% of the original train set and using
in O(N ) time.                                              the remaining 20% as a validation set. We then
                                                            retrain using the chosen hyperparameters on the
A.2    Attacks                                              full training set.
We use two heuristic attacks to compute an upper
                                                            A.4   Experimental details
bound for robust accuracy: one for ED1 pertur-
bations and one for internal permutations. Each             For our methods using transformers, we start with
heuristic attack is a beam search, with beam width          the pretrained uncased BERT (Devlin et al., 2019),
5. However, because |B(xi )| is very large for              using the same hyperparameters as the pytorch-
many tokens xi , even the beam search is intractable.       transformers repo.5 . In particular, we use the base
Instead, we run a beam search where the allow-              uncased version of BERT. We use a batch size of
able perturbations are B 0 (xi ) ⊆ B(xi ), where            8, and learning rate 2e−5. For examples where
|B 0 (xi )| << B(xi ) for sufficiently long xi . For        |Bα (x)| > 10000, we assume the prediction is not
our ED1 attack, we define B 0 (xi ) to be four ran-         robust to make computation tractible. Each typo
domly sampled perturbations from B(xi ) when                corrector uses the defaults for training from6 ; it is
the length of xi is less than five, and all deletions       trained on a specific task using perturbations of the
when xi is greater than five. Thus, the number of           training data as input and the true sentence (up to
perturbations of each word is bounded above by              OOV) as output. The vocabulary size of the typo
min{4, len(xi )−2}. For our internal permutations,          correctors is 10000 including the unknown token,
B 0 (xi ) is obtained by sampling five permutations         as in (Pruthi et al., 2019). The typo corrector is
at random.                                                  chosen based on word-error rate on the validation
                                                            set.
A.3    Datasets
                                                            A.5   Constrained adversaries
We use six out of the nine tasks from GLUE:
SST, MRPC, QQP, MNLI, QNLI, and RTE, all                    Using RobEn, since we can tractably compute ro-
of which are classification tasks measured by ac-           bust accuracy, it is easy to additionally consider
curacy. The Stanford Sentiment Treebank (SST-               adversaries that cannot perturb every input token.
2) (Socher et al., 2013) contains movie reviews             We may assume that an attacker has a budget of
that are classified as positive and negative. The           b ≤ L words that they may perturb as in (Pruthi
Microsoft Research Paraphrase Corpus (MRPC)                 et al., 2019). Exiting methods for certification (Jia
(Dolan and Brockett, 2005) and the Quora Ques-              et al., 2019; Huang et al., 2019) require attack to be
tion Pairs dataset4 contain pairs of input which are        factorized over tokens, and cannot give tighter guar-
classified as semantically equivalent or not; QQP           antees in the budget-constrained case compared to
contains question pairs from Quora, while MRPC              the unconstrained setting explored in previous sec-
contains pairs from online news sources. MNLI,              tions. However, our method lets us easily compute
and RTE are entailment tasks, where the goal is to          robust accuracy exactly in this situation: we just
predict whether or not a premise sentence entails           enumerate the possible perturbations that satisfy
a hypothesis (Williams et al., 2018). MNLI gath-            the budget constraint, and query the model.
ers premise sentences from ten different sources,              Figure 6 plots average robust accuracy across
while RTE gathers premises from entailment chal-            the six tasks using AGG C LUST as a function of
lenges. QNLI gives pairs of sentences and ques-             b. Note that b = 0 is simply standard accuracy.
tions extracted from the Stanford Question Answer-          Interestingly, for each dataset there is an attack only
ing Dataset (Rajpurkar et al., 2016), and the task is       perturbing 4 tokens with attack accuracy equal to
to predict whether or not the answer to the question        robust accuracy.
is in the sentence.                                         A.6   Number of representations
   We use the GLUE splits for the six datasets
and evaluate on test labels when available (SST-2,          We include here histograms for the datasets we did
MRPC), and otherwise the publicly released de-              not cover in the main body. The histograms for
velopment labels. We tune hyperparameters by                  5
                                                                https://github.com/huggingface/
                                                            pytorch-transformers
   4                                                          6
     data.quora.com/First-Quora-Dataset-Release-Question-       https://github.com/danishpruthi/
Pairs                                                       Adversarial-Misspellings

                                                        2763
                                                                                                MRPC and QQP are shown in Figure 7(a), while
                                                                                                the histograms for MNLI and QNLI are shown in
                                        Robust Accuracy for Constrained Adversaries             Figure 7(b). The fraction of x such that |Bα (x)| =
                    0.76                                                                        1 for each dataset and each set of encodings is
                                                                                                provided in Table 2.
                    0.75




 Rocust accuracy
                                                                                                A.7   Internal Permutation Results
                    0.74
                                                                                                We consider the internal permutation attack sur-
                                                                                                face, where interior characters in a word can be
                    0.73
                                                                                                permuted, assuming the first and last characters
                    0.72                                                                        are fixed. For example, “perturbation” can be per-
                                                                                                muted to “peabreuottin” but not “repturbation”.
                    0.71                                                                        Normally, context helps humans resolve these ty-
                                        0            1         2           3             4
                                                Allowable token perturbations (b)
                                                                                                pos. Interestingly, for internal permutations it is
                                                                                                impossible for an adversary to change the cluster
Figure 6: Robust accuracy averaged across all tasks                                             assignment of both in-vocab and out of vocab to-
based on different adversarial budgets b. b = 0 corre-                                          kens since a cluster can be uniquely represented
sponds to clean performance, and robust performance                                             by the first character, a sorted version of the inter-
is reached at b = 4                                                                             nal characters, and the last character. Therefore,
                                                                                                using C ONN C OMP encodings, robust, attack, and
                                                                                                standard accuracy are all equal. We use the attack
                                                                                                described in A.2 to attack the clean model. The
                                                                                                results are in Table 3.

                                            Sizes of Bα on MRPC    Sizes of Bα on QQP
                                  1.0
                                                                           C ONN C OMP




           Fraction of Examples
                                  0.8                        0.8           AGG C LUST


                                  0.6                        0.6

                                  0.4                        0.4

                                  0.2                        0.2

                                   0
                                            1   2   3–4 5–8 9+     1  2   3–4 5–8 9+
                                                           Size of Bα

                                            (a) Size of Bα over MRPC and QQP

                                            Sizes of Bα on MNLI    Sizes of Bα on QNLI
                                  1.0
                                                                           C ONN C OMP




           Fraction of Examples
                                  0.8                        0.8           AGG C LUST


                                  0.6                        0.6

                                  0.4                        0.4

                                  0.2                        0.2

                                   0
                                            1   2   3–4 5–8 9+     1  2   3–4 5–8 9+
                                                           Size of Bα

                                            (b) Size of Bα over MNLI and QNLI

Figure 7: Histograms showing sizes of Bα for MRPC,
QQP, MNLI, and QNLI.




                                                                                             2764
                        Encodings     SST-2    MRPC     QQP     MNLI    QNLI     RTE      Avg
                        Con. Comp.     86.9     71.6    72.7     45.3    54.6    40.4     61.9
                        Agg. Clust.    65.6     50.0    62.7     35.4    36.6    25.2     45.9

                      Table 2: Percentage of test examples with |Bα (x)| = 1 for each dataset.




             Accuracy           System          SST-2   MRPC     QQP     MNLI     QNLI      RTE    Avg
                          BERT                   93.8    87.7    91.2     84.3     88.9     71.1   86.2
             Standard
                          Con. Comp. + BERT      93.2    87.7    86.9     75.9     83.4     61.4   81.4
                          BERT                   28.1    15.9    33.0      4.9      6.2      5.8   15.7
             Attack
                          Con. Comp. + BERT      93.2    87.7    86.9     75.9     83.4     61.4   81.4
             Robust       Con. Comp. + BERT      93.2    87.7    86.9     75.9     83.4     61.4   81.4

Table 3: Results from internal permutation attacks. Internal permutation attacks bring the average performance
for BERT across the six listed tasks from 86.2 to 15.7. Our C ONN C OMP encodings, generated using the internal
permutation attack surface, achieve a robust accuracy of 81.4, which is only 4.8 points below standard accuracy.




                                                        2765
