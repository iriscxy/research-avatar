---
name: ideagen
description: Generate research ideas through a personalized lens (engineering/theory/benchmark) grounded in the researcher's own record AND the current literature, verify novelty against concurrent work, and present a ranked slate for the human to pick. Optionally build on a reference paper. Stops at the pick gate; the chosen idea goes to /workplan. Use when the user wants new research ideas, a research direction, brainstorming, or a literature survey. Invocable as /ideagen.
allowed-tools: Bash(*), Read, Write, Grep, Glob, WebSearch, WebFetch, AskUserQuestion
---

Read the **English** `aris-profile/PROFILE_AUTO.md` (via `$ARIS_PROFILE`) first — canonical; `PROFILE_AUTO.zh.md` is a human-facing mirror, not for logic. If it is absent, tell the user to run `/profile-construct`. Converse in **Chinese**; keep code/identifiers/paper-titles native.

**Arguments:** `<direction>` (free text) · `— lens: engineering|theory|benchmark` (= **mode**) · `— ref paper: <arXiv URL | PDF | URL>` (*optional*, triggers A0).

## Step 0 — Lock MODE + direction first
The mode decides what *kind* of idea is generated, so fix it BEFORE surveying — never infer silently, never start A1 first. If `— lens:` was passed use it; else **AskUserQuestion** for the mode (engineering = iterate habitual methods, reuse code · benchmark = survey / new dataset-eval / reproduce-and-beat · theory = a hypothesis/bound on the method-evolution fault line) and confirm `<direction>` at the same time (offer her *Niche subfields* if none given). Only once both are fixed do A0/A1/A2 run.

## Output conventions
- **One folder `outputs/` (create it), two-digit step prefixes.** This skill writes `outputs/00_REF_PAPER_SUMMARY.html` (only with `— ref paper:`) and the canonical `outputs/01_IDEA_REPORT.html` (landscape + ranked ideas in ONE file). Downstream continues the numbering (`02_EXPERIMENT_PLAN` … `05_FINDINGS`).
- **Every deliverable is self-contained HTML, never Markdown** — inline `<style>`, no external assets, real structure (`<h1>/<h2>`, `<table>`, `<ul>`). **Every paper reference is a direct `<a href>`** to its arXiv/DOI; unverifiable → visible `pending`, never a fabricated URL.
- **English first (canonical), then a faithful Chinese mirror.** Write each report in English → `outputs/01_IDEA_REPORT.html` (and `00_REF_PAPER_SUMMARY.html`), then produce a faithful Chinese translation `outputs/01_IDEA_REPORT.zh.html` (same structure, links, ids, scores; translate the prose, keep code/paper-titles native). **English is canonical — it is what `/workplan` reads, and the `SELECTED` stamp lives in the English file** (mirror it into the `.zh.html`). The Chinese mirror must be **natural, native Chinese — a faithful sentence-level translation, never word-for-word machine translation** (which yields unreadable collocations); re-read each sentence. **Address the researcher in the SECOND person — 你 / 你的, never third person 她 / 她的.** The report is written to her, so English "her stack / her Flipping-KD / her profile" becomes 你的技术栈 / 你的 Flipping-KD / 你的画像; third-person 她 reads as if talking about a stranger and is wrong. (Converse with the researcher in Chinese as before; only the written report is English-canonical.)

## A0 — Reference paper (only if `— ref paper:` given)
Summarize BEFORE surveying so generation targets its gaps. Fetch (`tools/fetch_fulltext.py` / `pdftotext` / WebFetch), then write `outputs/00_REF_PAPER_SUMMARY.html`: **what they did · key results · limitations & open questions · improvement directions**. Numbers/claims trace to the paper's text; if you couldn't fetch it, say so and ask for the PDF.

## A1 — Literature survey (grounding — do NOT skip)
Search arXiv + Semantic Scholar + Google Scholar (`tools/` helpers; WebSearch for breadth) for recent work (~2 years) on `<direction>`. **Cover it with MANY distinct query angles — not one or two searches.** Hit the sub-directions, the dominant methods, evaluation/benchmarks, safety/robustness, and adjacent applications separately; a real survey is **typically 30–50 papers**, not a handful. Keep searching new angles until fresh queries stop surfacing new relevant work (saturation). Build a short landscape: **sub-directions · dominant approaches · recurring limitations · structural gaps**. **Anti-hallucination:** cite only papers you actually retrieved; unverifiable → `[UNVERIFIED]`, never fabricate an id/DOI/title. Write the *Literature Landscape* as the opening section of `outputs/01_IDEA_REPORT.html` (prose summary + inline links to the key papers). The **full list of ALL surveyed papers goes at the very END of the file as an appendix** (not inline in the landscape) — one row per paper: title (direct `<a href>` to arXiv/DOI) · year · one-line relevance/takeaway. **Group the appendix into a few (≈4–6) major categories**, each with an `<h3>` header + a one-line description + its own `<table>` — NOT one flat table. Categories should mirror the survey angles (e.g. architecture/routing · interpretability · safety · efficiency · reasoning/RL/domain), adapted to the `<direction>`; put the paper count in each header and note which category each top idea draws from. The prose landscape summarizes; the categorized appendix is the complete evidence list, so the researcher can see everything the ideas were grounded on, organized, without it interrupting the ideas.

## A2 — Idea generation (ONE lens, method-first)
Generate 6–8 candidates through the chosen mode, seeded by the profile AND the A1 gaps:
- **engineering** — iterate habitual methods from *Dominant Methods*; prefer reuse-existing-code. No arbitrary time cap ("2-week pilot"); let the problem set the scope.
- **theory** — read *Publications Index* in time order + *Research Lineage*; find the method-evolution fault line; propose hypotheses adjacent to, not repeating, the lineage.
- **benchmark** — from *Active Venues* + *Niche Subfields*: follow-up **survey** / **custom benchmark-dataset** filling an eval gap / **reproduce-and-beat** a recent competitor.

**One-sentence lead + method-first.** Every idea opens with **ONE clear sentence** that says what it is — concrete about mechanism and contribution, readable in a single pass. Write it **for an experienced peer**: use the correct domain terms precisely (do not avoid them), but do NOT hide behind abstract methodology-speak ("steering as a measurement tool for mechanistic faithfulness") NOR dumb it down with childish analogies (kids arguing about vaccines). If the one sentence isn't clear, the idea isn't ready. Only *after* that sentence give the **2–4 concrete method steps** (what we build/train/run), then hypothesis/expected-outcome as acceptance criteria.

**Re-run = accumulate, never discard.** A re-invocation does NOT replace the prior slate with an all-new one. Read any prior `outputs/01_IDEA_REPORT.html`, **carry ALL its ideas forward** (they remain valid candidates — an un-selected idea is not a failed idea), then **ADD fresh candidates** by rotating to a different profile asset / gap, and **re-rank the union**. The pool grows across runs. Mark the new ones (`🆕 new this run`) so the researcher sees what changed. Only drop an idea the researcher *explicitly rejected* as a direction. Do NOT manufacture a gimmicky twist / far-fetched mashup / contrived problem just to add something new: if this run's honest fresh angles turn out crowded or weaker than the carried-forward ideas, say so explicitly and let the strong prior ideas keep their rank rather than padding the slate.

**≥2–3 of the slate must be `novel` (not just `differentiable`)**, else too few real options. If short, keep widening (new gap × new asset) until you reach it — but never fake the count or mislabel `differentiable` as `novel`; if honest widening truly can't reach it, present what you have and say so.

## A3 — Novelty check (annotate, do not narrow)
Per surviving candidate: (1) **vs her OWN work** — flag anything that merely restates a paper she published; (2) **vs the field incl. last 3–6 months** — record closest paper + differentiation point. Verdict `novel` / `differentiable (needs framing)` / `already exists`; unverifiable → `[UNVERIFIED]`. This is an **annotation** — only a genuine exact duplicate is dropped here; quality narrowing happens at A4 + the human gate, never by silent deletion.

## A4 — Objective gate · scorecard · devil's-advocate ranking
Separate the objective filter from the subjective ranking; the generator's taste does NOT do the quality narrowing (borrowed from autoresearch).
1. **Objective gate (mechanical only):** drop a candidate only on an objective fact — compute clearly beyond the profile's hardware, or a provably-unavailable dataset. Never drop on "looks complex" / "might already be done" (annotate instead).
2. **Scorecard per idea** (transparent, reproducible): **Novelty** `X/10` + closest work + differentiation · **Feasibility** (compute/data/impl vs the profile stack) · **Risk** LOW/MED/HIGH · **Contribution type** (empirical / method / benchmark / theory / diagnostic) · **Fit** (which *Dominant Method*/niche) · **Single-mechanism test** (below) · **Reviewer's likely objection** (strongest top-venue objection) · **rough effort** (honest).
2a. **Single-mechanism test — the anti-"boring mashup" gate (apply to EVERY idea).** State the idea's ONE core mechanism in a single sentence, then try to break the idea into independent contributions. **If it decomposes into "improvement A + improvement B" (e.g. `technique X` *plus* `apply it to domain Y`), where each half is a separate increment a reviewer could accept or reject on its own, the idea has no single core — it is a boring mashup and must NOT ship in that form.** Fix it, don't pad it: reforge around one insight so the parts stop being separable — the second "contribution" should become a *consequence or a falsification test* of the first, not a bolt-on module. (E.g. "defense A + also works on audio" is a mashup; "harmful intent lives on one modality-invariant axis, so the same conditional defense transfers to audio *for free* — and if it needs a separate audio module, the core claim is false" is one mechanism whose audio result is the test of the claim, not a second feature.) A candidate that survives only as A+B gets **capped at `differentiable`, never `novel`**, and is downranked below any true single-mechanism idea; note the decomposition explicitly in its card so the human sees why. This is *not* a licence to invent a grand unifying theory over a genuinely engineering idea — the reforged core can be a concrete engineering mechanism — it just has to be ONE thing.
3. **Devil's-advocate ranking:** run one adversarial pass over the full set (strongest objection · likely failure mode · **does it pass 2a or is it an A+B mashup** · real collision or differentiable · how to rank for a top venue · which 2–3 to actually work on) and **rank by that verdict, not first-pass enthusiasm.** Prefer a **cross-model** reviewer — send the set to `codex` via `mcp__codex__codex` — else an explicit same-model pass (write the objections out; never skip).

## GATE — human is judge
Present **5–7 ranked ideas** in `outputs/01_IDEA_REPORT.html` in A4 rank order (ranked `<table>`), **≥2–3 with a `novel` verdict**. Give each a stable id `I1, I2, …` in the first column (this is how the pick is referenced downstream). **Each idea — in both the table and its card — leads with its A2 one-sentence pitch** (peer-level, concrete, no jargon-fog / no childish analogy); the card then adds its A4 scorecard plus method steps · hypothesis · MVE · why-novel-vs-own-work. The AskUserQuestion options must use that same one-sentence pitch, not a vaguer paraphrase. **Stop. AskUserQuestion for the researcher to pick / kill / redirect** (offer ideas by id + one-line title). Never auto-proceed.

**Persist the pick:** after she picks, stamp `outputs/01_IDEA_REPORT.html` — a top banner `Selected: I<k> — <title>` (with date) + a `✅ SELECTED` tag on that row. If she redirects instead, regenerate and leave no stale stamp.

**Handoff:** the chosen idea → **`/workplan`**, which reads the stamp and writes `outputs/02_EXPERIMENT_PLAN.html`. Do not write the experiment plan here.
